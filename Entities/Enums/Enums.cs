namespace Entities.Enums
{
    public enum ShipmentStatuses
    {
        Received,
        Preparing,
        Shipped
    }
    public enum PaymentMethods
    {
        Cash,
        CreditCard,
        DebitCard,
        BankTransfer,
        Check
    }
    public enum Roles
    {
        OfficeWorker,
        Seller
    }
}
